{
  "name": "novel-reader",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jsdom": "^24.0.0",
    "supertest": "^7.0.0",
    "@mozilla/readability": "^0.5.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
